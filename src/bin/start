#!/usr/bin/node --harmony --use-strict

let mode = '!!BUILD!!';

let app = require('../server/app')(mode);

let server = app.listen(mode === 'prod' ? 80 : 5000, function() {
  console.log('Express server listening on port ' + server.address().port);
});
